# AWS ECS profile - minimal overrides
# Activates when SPRING_PROFILES_ACTIVE=ecs
# Main configuration is in application.yml

# Enable debug logging for AWS discovery
logging:
  level:
    com.hazelcast.aws: DEBUG
