apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-playground.fullname" . }}
  labels:
    {{- include "spring-playground.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-playground.selectorLabels" . | nindent 4 }}
